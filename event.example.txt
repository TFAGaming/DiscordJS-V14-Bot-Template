const ExtendedClient = require('../../class/ExtendedClient');

module.exports = {
    event: '',
    once: false,
    /**
     * 
     * @param {ExtendedClient} client
     * @param {*} args... 
     * @returns 
     */
    run: (client, args...) => {
        
    }
};